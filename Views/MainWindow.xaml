<Window x:Class="Hromiak_WPF_project.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hromiak_WPF_project.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="500" MinHeight="250" MinWidth="200">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Ім'я користувача:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" Text="{Binding User.Username, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2" Text="Дата народження:" FontWeight="Bold" Margin="0,20,0,5"/>
        <DatePicker Grid.Row="3" SelectedDate="{Binding User.BirthDate, UpdateSourceTrigger=PropertyChanged}" />

        <Button Grid.Row="4" Content="Розрахувати" Command="{Binding CalculateCommand}" Height="30" Margin="0,20,0,0"/>
    </Grid>
</Window>
